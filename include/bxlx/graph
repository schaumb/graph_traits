#include <type_traits>

namespace bxlx::graph {
    enum class graph_representation_t {
        adjacency_list = 1,
        adjacency_matrix,
        edge_list,
    };

    template<class T>
    constexpr graph_representation_t graph_representation = [] {
        return graph_representation_t{};
    } ();
}